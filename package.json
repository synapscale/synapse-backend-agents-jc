{
  "name": "synapscale-backend",
  "version": "1.0.0",
  "description": "SynapScale Backend - PostgreSQL with SQLAlchemy",
  "scripts": {
    "start": "python -m uvicorn src.synapse.main:app --host 0.0.0.0 --port 8000",
    "dev": "python -m uvicorn src.synapse.main:app --host 0.0.0.0 --port 8000 --reload",
    "test": "pytest"
  },
  "dependencies": {
    "dotenv": "^16.5.0"
  }
}

